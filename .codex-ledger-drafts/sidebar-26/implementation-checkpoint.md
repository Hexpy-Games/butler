# #26 구현 체크포인트 — 2026-09-07

## 최신 상태 — P1–P7 구현·직접 리뷰 완료

아래의 시간순 기록은 과거 체크포인트다. 현재 증거와 잔여 사항의 권위는 `validation-report.md`다.
실제 Electron/모델에서 스마트 그룹, 문맥 분리, 인라인 원문 참조, 일반↔프로젝트 이동 후 실행 환경을 검증했다.
자연어 분리 도구의 effect adapter와 App/external/canonical ID 연결 누락을 수정한 뒤 실제 성공을 확인했다.
최신 요청의 하단 아이콘 순서도 적용·검증했다. 전체 약 90%; main 병합과 운영 반영을 진행한다.

직접 구현 중. 서브에이전트 사용 없음. 브랜치 `codex/sidebar-26`.
기존 승인 목업/DS 변경을 보존했다. 아직 commit, merge, push, 운영 재시작하지 않았다.
사용자 승인 조건은 전체 구현 + 검증 후 운영 반영이며, 아래 단위 검사 통과는 완료 판정이 아니다.

## 구현 및 확인

- 혼합 그룹/배치 API, DB 재시작 보존, 범위·순환 검증, undo와 UI 그룹 생성/이동.
- 일반/프로젝트 이동 준비 및 binding identity CAS, canonical 현재 scope 동기화, App DB 전진 복구.
- Lexical 0.50.0 실제 컴포저 및 인라인 세션 참조 노드. 브라우저 참조 메뉴 삽입/undo/redo 확인.
- 메시지·대기열·웹/Electron 초안에 content parts 저장, canonical user message parts 보존.
- 명시적 참조의 서버 ID 해석, 제한된 최근 내용과 원문 읽기 도구 전달; 요청 identity에 참조 포함.
- 서버 접수 전 초안 제거 방지. 편집기 ref 재연결로 메뉴 포커스를 빼앗던 문제 수정.
- 분리 대화 seed/target 예약, prepared 세션의 실행 금지, 실패 후 동일 예약 재개 구현 중.
- 검색 NFKC/다중 단어/소속 경로/제목 우선순위, 보관 프로젝트 하위 검색 제외.

## 검사 증거

- 그룹/이동/참조/권한 UI 집중 테스트: 11개, 88 assertions 통과.
- 분리 대화 예약/실패 복구 단위 테스트: 1개, 12 assertions 통과.
- TypeScript typecheck 및 git diff --check 통과 (이 체크포인트 직전).
- 웹 프로덕션 UI build 통과. bundle 크기 경고는 남아 있다.
- 브라우저 스크린샷: `/tmp/butler26-reference-desktop.png`.
- 격리 App URL `http://127.0.0.1:58381/`은 운영이나 실제 모델 증거가 아니다.

## 남은 구현·검증

- P2: 실제 Work n/k 표시, 행별 안정 selector/인덱스, 공유 시간 갱신, 전체 메뉴/키보드/모바일/레이아웃 회귀.
- P3: 실제 native drag 저장 및 중복 즐겨찾기 마커 E2E, sticky 조상 실화면 검사.
- P4: context gate 전체 진입/재시도/종료 소유권 리뷰, 준비 중 crash의 안전한 worktree 정리,
  이동 다음 실제 모델의 새 프로젝트 지식/작업 위치 검사.
- P5: 실제 문장 중간 drop, 선택 치환, 원자 삭제, clipboard, 한글 IME, 첨부·실패·대화 전환 회귀,
  canonical projection 및 실제 모델 원문 읽기, dependency lockfile 정합성.
- P6: 답변 액션/내부 도구/실행기, seed UI·출처 복귀·프로젝트 생성은 연결했다. 실제 모델 E2E와 원문 창 이동 검증이 남아 있다.
- P7: 후보 인덱스/첫 메시지 후 비차단 분류/설정/동시성/undo 구현. 지연 응답·수동 배치 보존 검사 통과. 실제 모델 E2E가 남아 있다.
- P8: 전 범위 테스트·DS lint·전체 직접 리뷰·실제 웹/Electron/모바일/모델 E2E 후 병합·운영.

## 제공자 정책 확정 — 2026-09-07

사용자는 같은 모델을 사용하고 3초 제한 없이 응답을 기다리도록 결정했다.
Codex 구독도 포함하며 출력 128토큰은 지침일 뿐 강제 상한이 아니다.
분류 전용 deadline은 없고 서비스 종료 signal 및 기존 공급자 오류 처리를 사용한다.
일반 대화 처리는 분류를 기다리지 않으며 늦게 도착한 결과도 현재 배치/수동 이동 여부를 검증한다.
스펙 r3와 실행 계획에 반영했다. 제공자 제약은 더 이상 사용자 결정 대기 사유가 아니다.

## 추가 검증 체크포인트

- 전체 약 60%: P1–P7 호출 경로 구현 후 남은 세부 회귀를 수정 중이며, P8의 실제 클라이언트·모델·병합·운영 검증은 포함되지 않았다.
- 집중 회귀 9개 / 98 assertions 통과. 3초 초과 응답의 정상 적용, 모델 호출 중 수동 배치 변경의 보존 포함.
- 큰 단일 문자열의 토큰 계산 CPU 급증을 확인하여 256자 조각별 보수적 추정으로 입력 포장 비용을 제한했다.
- 사이드바 행별 selector/children index, 공유 분 단위 시계, 승인된 계획 진척도, 기존 UI 캐시의 탭/접힘 복원 추가.
