# Butler Progress Projection

This package owns the browser-safe event-to-progress-row projection and the
append-only work-block reducer shared by Butler Agent persistence and Butler App
live rendering. It has no runtime dependency on either product package.
