{
  "name": "butler-telegram-commands",
  "version": "0.0.2",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "${BUTLER_BUN:-bun} install --no-summary && ${BUTLER_BUN:-bun} server.ts",
    "test": "bun test server.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "grammy": "^1.21.0"
  }
}
