/**
 * steward-liveness.ts — native-primary steward watchdog placeholder.
 *
 * Native steward routing is driven by session-store state and explicit
 * turn handling, so there is no terminal-session health to inspect here.
 */

export async function checkStewardLiveness(): Promise<void> {
  return;
}
