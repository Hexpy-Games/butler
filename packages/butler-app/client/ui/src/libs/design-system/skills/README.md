# Design System Skills

This directory contains agent-facing skills that travel with the Butler Design System.

Copy `butler-design-system` into a Codex or Butler skills directory when an agent should build UI against the project design system.

`butler-design-system` is the canonical skill name. It intentionally lives
inside the design-system tree so the skill, component docs, fixtures, and public
API travel together like a small subrepo.
