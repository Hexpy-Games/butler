# ChangedLineDiff

Presenter-only changed-line rows with old/new line numbers and addition/deletion tones.
Callers provide already-bounded changed lines and an accessible region label.
Rows share one column grid and full content width inside a horizontal scroller.
Code uses DS typography without per-line mobile text inflation; browser zoom remains available.
