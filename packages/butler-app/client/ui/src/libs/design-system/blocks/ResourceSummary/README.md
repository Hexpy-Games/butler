# ResourceSummary

`ResourceSummary` renders resource identity content: optional icon, title,
description, and metadata. It does not draw a card surface.

Use it inside resource-shaped containers such as `ResourceTile` and
`DocumentTile` when they need the same text hierarchy without nesting one card
inside another.
