# InlineReference

Inline, token-colored reference to a document or conversation. Compose an existing DS icon and a text label. It participates in the surrounding text baseline without a chip background or control padding. Provide `onClick` for navigation; omit it inside an editable decorator. `unavailable` lowers its text hierarchy without removing the historical label. Domain identity and navigation remain outside the DS.
